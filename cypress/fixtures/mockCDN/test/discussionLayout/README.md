This test mostly checks that we're able to lay out the various call tiles. In order not to actually contact the daily server, we set checkAudio and checkVideo to false in the config.

If you want to run this test manually, and actually have videos display, remember to set those flags to true.
