---
type: openResponse
---

# Globals propagation probe

This prompt body contains a relative-path markdown image so the L3 spec
can pin that `recruitingBatchConfig.cdnURL` reached the client and is
applied when stagebook rewrites markdown image URLs.

![probe](globals_probe.png)

---

> Your answer
