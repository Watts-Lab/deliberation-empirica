# For deployment

version: '3'
services: 
  empirica-prod:
    platform: linux/amd64  # needed when developing on apple silicon
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
