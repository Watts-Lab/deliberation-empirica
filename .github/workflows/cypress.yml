name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Start Empirica Server
        run: docker-compose -f docker-compose-cypress.yml up --abort-on-container-exit
